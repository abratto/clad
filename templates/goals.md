<!-- Template for Stage 00 (00_actor-goal). Purpose: see methodology/implementation/STAGES.md §"Stage 00 — Actor / Goal". -->

# Goals

> One row per goal. Phrase each as `<Actor> wants to <do something> so
> that <reason>`. The reason matters — it is what stage 1's use case
> turns into the operational principle.

| Actor | Goal | Priority | In scope? |
|---|---|---|---|
| `<ActorName>` | wants to <do X> so that <reason> | P0 \| P1 \| P2 | yes \| no |
| `<ActorName>` | wants to <do Y> so that <reason> | P0 \| P1 \| P2 | yes \| no |

## Out of scope

> Explicit list of goals that came up but are deliberately excluded.
> Restate the reason briefly.

- `<ActorName>` wants to <do Z> — out of scope because <reason>.
