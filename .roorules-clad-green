# Mode: CLAD 04c–04e — Green Implementation

## What you are doing
Writing the implementation class that makes approved red tests pass.
The tests have already been approved by the human. Do not modify them.

## Mandatory sequence — do not skip or reorder
1. Read ALL approved test files for this concept.
2. Extract and write down:
   - Exact package declaration
   - Exact class name being instantiated
   - Every method signature called (name, parameter types, return type)
   - Every inner class and enum referenced (name, fields accessed)
3. Read `.roo-clad-config` (repo root) to get TEST_COMMAND and STORAGE_LAYER.
   If the file does not exist, stop and ask the human to create it by
   copying `.roo-clad-config.example` and filling in their values before continuing.
4. Read `features/UC-XX-<slug>/_config/package-and-layout.md` to get
  APP_PACKAGE_ROOT and APP_SOURCE_ROOT.
  If this file is missing or not filled, stop and ask the human to
  set it before continuing.
5. Read `04_implement/04a_storage-mapping/output/` for the profile's storage layer shape.
6. Read `04_implement/04b_spec/output/` for the SPEC — list every defined outcome.
7. Write the implementation to match steps 2–6 exactly.
  Do not copy package/source paths from `reference-impl/` unless they
  match APP_PACKAGE_ROOT and APP_SOURCE_ROOT.
8. Run the tests using the TEST_COMMAND from `.roo-clad-config`.
   If any test fails, read the failure output and fix. Repeat until all green.
9. Stop. Present the implementation to the human. Wait for approval.

## Local-context discipline (Roo)
- Keep intelligent context condensing enabled; use an early threshold
  (about 80%) for long sessions.
- Treat `features/UC-XX-<slug>/RESUME.md` as live working memory during
  active stages.
- At the end of each turn, update `RESUME.md` with current blocker,
  failing command, files touched, and next 1-3 concrete steps.
- Do not copy long test/output bodies into chat unless specifically
  requested by the human.

## Hard constraints (checked before every file write)
- Package and class name MUST match what the tests import and instantiate (step 2).
- Implementation file path and package MUST match
  APP_SOURCE_ROOT and APP_PACKAGE_ROOT from
  `features/UC-XX-<slug>/_config/package-and-layout.md`.
- `com.example.app` is forbidden unless APP_PACKAGE_ROOT is explicitly
  set to `com.example.app` in that config file.
- No constructor signature mismatch — if tests call `new Account()`, the class
  must have a no-arg constructor.
- No two constructors or methods with the same erasure (Java compile error).
- No `Map.of()` mutations — `Map.of()` is unmodifiable; use `HashMap` if you
  need to add entries after construction.
- No in-memory substitutes (e.g. HashMap) for the storage layer.
  Use the technology specified by STORAGE_LAYER in `.roo-clad-config`
  (and confirmed by `04a/output/`).
- Every SPEC outcome gets its own distinct code path (R9). Do not collapse
  two outcomes into one return value.
- Every public action emits exactly one flow token at completion (R5).
- No imports across concept packages (R1).
- Do not claim a green stage from markdown artefacts alone; the required
  source files and executed test command evidence must exist.
- In `04e`, implement exactly the approved Stage `03` sync set. Do not
  invent extra coordinator/sync classes that lack an upstream sync spec.
- Do not continue into the next Stage `04` sub-stage without explicit
  approval for the current green work.

## Outcome branching checklist
For each outcome in the SPEC, verify:
- [ ] It has its own `if` / `switch` branch — not shared with another outcome
- [ ] It returns the correct `OutcomeType` enum value
- [ ] `message` is null on success outcomes, non-null on failure outcomes
- [ ] `id` fields are non-null on creation success, null on failure

## ⚠ Mode switching — read carefully

You are in `clad-green`. You must NOT switch to `clad-red` on your own.

**Switch back to `clad-red` only when the human explicitly says one of:**
- "approved — proceed to [04e / next stage / sync tests]"
- "implementation approved"
- "proceed to 04e"
- "proceed to [next stage]"

The following are NOT approval and must NOT trigger a mode switch:
- Silence
- A question about the implementation
- "looks good" without "approved" or "proceed"
- The human restating what you wrote back to you

If you are unsure whether approval was given, ask: "Is the implementation
approved? I'll switch back to clad-red once you confirm."

## You are done when
All tests are green AND the human explicitly approves the implementation
as described above.
